# TODO

Generated by the weekly docs verification workflow. Last updated: 2026-07-15
(seeded manually; the Monday run regenerates this file). Items are carried
forward, re-prioritized, or dropped each Monday based on the current state of
the codebase. No "Done since last week" section yet - this is the first run.

## P1 - Correctness and blockers

- [ ] CLAUDE.md sections 3 and 8 claim `.github/workflows/{azure-dev,tests,eval-nightly}.yml` exist ("PR CI: tests.yml", "Nightly: eval-nightly.yml"); only `pages.yml` and `update-claude-md.yml` are on disk. Either add the missing workflows or remove the claims.
- [ ] CLAUDE.md section 3 lists ~16 files under `docs/` (architecture.md, localdev.md, data_ingestion.md, etc.) and several sections link to them; `docs/` actually contains only `hierarchical_agent_teams_template.ipynb` and `images/`. Write the docs or fix the references.
- [ ] CLAUDE.md describes `site/` as a Jekyll site (_config.yml, Gemfile, _layouts, _posts, jekyll-* plugins); `site/` contains only `index.html`. Reconcile sections 2, 3, and 10 (the `DEMO_URL` gotcha) with `.github/workflows/pages.yml`.
- [ ] CLAUDE.md section 3 claims `.devcontainer/` and `ps-rule.yaml` at repo root; neither exists. Remove or restore.
- [ ] Scripts drift: CLAUDE.md lists `setup_cloud_ingestion.{ps1,py}`, `auth_init.{ps1,py}`, `auth_update.{sh,ps1,py}`, `auth_common.py`, `roles.ps1`, `load_python_env.{sh,ps1}`, `cost_alerts.bicep`, and `app/start.ps1`; none exist. `scripts/fetch_learn.py` exists but is undocumented.
- [ ] Infra drift: CLAUDE.md claims `infra/main.test.bicep`, `backend-dashboard.bicep`, `network-isolation.bicep`, `private-endpoints.bicep`; none exist. `infra/app/rbac.bicep` exists but is undocumented.
- [ ] CLAUDE.md section 3 claims `tests/test_prepdocslib_pdfparser.py`; the file does not exist (only `test_prepdocslib_textsplitter.py` does).
- [ ] Frontend drift: CLAUDE.md claims `src/router.tsx` and `src/lib/theme.ts`; neither exists (`theme.ts` lives at `src/theme.ts`, `src/lib/` has only `cost.ts` and `deepLink.ts`).

## P2 - In-flight and target work

- [ ] Implement `CloudIngestionStrategy.setup/run` in `app/backend/prepdocslib/cloudingestionstrategy.py` - both methods are TODO log-only stubs (POST indexer + skillset spec; trigger indexer run).
- [ ] Implement `IntegratedVectorizerStrategy.setup/run` in `app/backend/prepdocslib/integratedvectorizerstrategy.py` - both methods are TODO log-only stubs (wire AI Search vectorizer; orchestrate indexer via SDK).
- [ ] CLAUDE.md section 13 marks `core/cache.py` as planned for Azure Managed Redis, but `app/backend/core/semantic_cache.py` and `core/conversation_memory.py` already exist - reconcile the integration matrix with the real module names.
- [ ] `core/pgvector.py` (PostgreSQL pgvector + azure_ai secondary analytics) is marked planned in CLAUDE.md section 13 and does not exist - implement or descope.
- [ ] Document the undocumented backend surface in CLAUDE.md sections 2-4: `chainlit_app.py` + `chainlit.md` + root `.chainlit/`, `youtube_service.py`, `agents/{tutor_agent,hierarchical_graph,skills,notes_search_tool,run_eval}.py`, `approaches/hierarchical_multiagent_approach.py`, `voice/{voice_live,transcript_cleaner}.py`, and `core/{aoai_client,conversation_memory,cosmos_vector_retriever,document_retriever,embeddings_client,resilience,semantic_cache}.py`.
- [ ] Document the undocumented root-level content: `claude-md-memory-workflow/`, `self-documenting-ai-agent/`, `context-engineering-workflow.md`, `data/learn/`, and `data/blog_notes.sqlite`.

## P3 - Hygiene and hardening

- [ ] Add dedicated tests for `approaches/hierarchical_multiagent_approach.py` and `agents/tutor_agent.py` - no `tests/test_hierarchical*.py` or `tests/test_tutor*.py` exists.
- [ ] Add tests for the stub ingestion strategies once implemented (`cloudingestionstrategy.py`, `integratedvectorizerstrategy.py`) - currently untested log-only code.
- [ ] i18n: CLAUDE.md claims a "folder skeleton for other locales"; only `app/frontend/src/locales/en/` exists. Add the skeleton or drop the claim.
- [ ] `agents/eval_questions.jsonl` and `agents/run_eval.py` live inside the backend package while the eval harness lives in `evals/` - decide where agent evals belong and document it.
