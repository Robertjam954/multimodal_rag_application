{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "environmentName": { "value": "${AZURE_ENV_NAME}" },
        "location": { "value": "${AZURE_LOCATION}" },
        "principalId": { "value": "${AZURE_PRINCIPAL_ID}" },
        "foundryName": { "value": "${AZURE_FOUNDRY_NAME=ai-tutor-foundry}" },
        "foundryProjectName": { "value": "${AZURE_FOUNDRY_PROJECT_NAME=ai-tutor-ms-azure-proj}" },
        "cosmosAccountName": { "value": "${AZURE_COSMOSDB_ACCOUNT=cosmosdbaitutor7a79c7}" },
        "storageAccountName": { "value": "${AZURE_STORAGE_ACCOUNT=azureblobstorageai}" },
        "keyVaultName": { "value": "${AZURE_KEY_VAULT_NAME=ai-tutor}" },
        "chatGptDeploymentName": { "value": "${AZURE_OPENAI_CHATGPT_DEPLOYMENT=gpt-4o-mini}" },
        "embeddingDeploymentName": { "value": "${AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-large}" },
        "useAzureSearch": { "value": "${USE_AZURE_SEARCH=false}" },
        "searchServiceName": { "value": "${AZURE_SEARCH_SERVICE=}" },
        "searchIndexName": { "value": "${AZURE_SEARCH_INDEX=rag-index}" },
        "searchSkuName": { "value": "${AZURE_SEARCH_SKU=basic}" },
        "searchSemanticSearch": { "value": "${AZURE_SEARCH_SEMANTIC_RANKER_TIER=free}" },
        "documentRetriever": { "value": "${CLOUD_DOCUMENT_RETRIEVER=cosmos}" }
    }
}
